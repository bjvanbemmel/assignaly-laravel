<template>
    <select
        class="border border-gray-300 bg-gray-50 py-1.5 px-4 text-sm hover:bg-gray-100 hover:cursor-pointer"
    >
        <option
            v-for="option, key in options"
            :key="key"
        >
            {{ option.label }}
        </option>
    </select>
</template>

<script>
export default {

    props: {
        options: {
            type: Array,
            required: true,
        },
    },
}
</script>
